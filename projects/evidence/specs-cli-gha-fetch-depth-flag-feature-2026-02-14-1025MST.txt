Evidence: specs gha --fetch-depth <n> flag feature (TDD)

What shipped:
- Added `--fetch-depth <n>` to `specs gha ... --workflow` to control `actions/checkout@v4` fetch depth.
- Deterministic validation:
  - `WORKFLOW_FETCH_DEPTH_REQUIRES_WORKFLOW` (exit=2) when used without `--workflow`
  - `WORKFLOW_FETCH_DEPTH_INVALID` (exit=2) when <0 or non-integer
- Workflow YAML now emits:
  - `- uses: actions/checkout@v4` + `with:` + `fetch-depth: <n>`
- Added golden fixtures:
  - `tests/fixtures/gha/workflow-fetch-depth-0.yml`
  - `tests/fixtures/gha/workflow-fetch-depth-2.yml`

Test run:
- `bun test` -> 114 pass / 0 fail (Ran 114 tests across 6 files.)
