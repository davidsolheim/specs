Specs CLI feature evidence — `specs gha --workflow --runs-on <label>`

Feature:
- Added `--runs-on <label>` to `specs gha ... --workflow` so operators can set the GitHub Actions runner label (e.g. `self-hosted`).
- Validation (deterministic):
  - `--runs-on` without `--workflow` => exit=2, stderr: `WORKFLOW_RUNS_ON_REQUIRES_WORKFLOW`
  - `--workflow --runs-on "   "` => exit=2, stderr: `WORKFLOW_RUNS_ON_INVALID`

Tests:
- bun test summary:
  89 pass
  0 fail
  Ran 89 tests across 6 files.

Git:
- commit: 4940db21a3c6485ac6041bcaae6eadd952ed8bee
- branch: elias (pushed to origin/elias)
