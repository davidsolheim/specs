# @sitespecs/analyzer-core

Reusable local-first analysis engine for the SiteSpecs CLI and platform services.

This package is published so other SiteSpecs codebases can consume a versioned analysis core from npm, but most end users should install [`@sitespecs/specs`](https://www.npmjs.com/package/@sitespecs/specs) instead.
