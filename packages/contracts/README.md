# @sitespecs/contracts

Shared response, queue, and webhook contracts for SiteSpecs packages.

This package is published so the CLI, workers, and web platform can consume the same versioned contract surface from npm.
