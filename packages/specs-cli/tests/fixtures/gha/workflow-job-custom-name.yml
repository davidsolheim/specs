name: SiteSpecs
on:
  workflow_dispatch:
jobs:
  sitespecs:
    name: My Specs CI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Specs CI
        run: npx -y @sitespecs/specs@next ci example.com --baseline baseline.json --fail-on-diff
